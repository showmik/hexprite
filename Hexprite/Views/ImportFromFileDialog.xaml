<Window x:Class="Hexprite.Views.ImportFromFileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:core="clr-namespace:Hexprite.Core"
        Title="Import from File"
        Width="600" Height="500"
        MinWidth="450" MinHeight="400"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.Surface.Base}"
        FontFamily="{DynamicResource Font.Primary}"
        ShowInTaskbar="False"
        WindowStyle="None"
        AllowsTransparency="False"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        core:WindowChromeHelper.ForceRoundedCorners="True">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="34"
                      CornerRadius="0"
                      GlassFrameThickness="0"
                      ResizeBorderThickness="5"
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Border Style="{StaticResource DialogWindowBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- TITLE BAR -->
            <Border Grid.Row="0" Background="{DynamicResource Brush.Surface.MenuBar}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}"
                               Foreground="{DynamicResource Brush.Text.Muted}"
                               FontSize="12" VerticalAlignment="Center"
                               Margin="8,0,0,0" IsHitTestVisible="False"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button Click="CaptionClose_Click" ToolTip="Close" Width="44" Height="34"
                                Style="{StaticResource CaptionCloseButtonStyle}">
                            <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="10"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Title -->
                <TextBlock Grid.Row="0"
                           Text="IMPORT FROM FILE"
                           Style="{StaticResource HeaderStyle}"
                           FontSize="14"
                           Margin="0,0,0,8" />

                <!-- Instructions -->
                <TextBlock Grid.Row="1"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource Brush.Text.Muted}"
                           FontSize="12"
                           Margin="0,0,0,8"
                           Text="Select a C/C++ or Python source file. Hexprite will scan it for sprites and create a dynamic link." />

                <!-- File Picker -->
                <Grid Grid.Row="2" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="TxtFilePath" Grid.Column="0" IsReadOnly="True" Margin="0,0,8,0"
                             Style="{StaticResource ModernTextBoxStyle}"
                             Text="{Binding SelectedFilePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Grid.Column="1" Content="Browse..." Click="BrowseBtn_Click"
                            Style="{StaticResource ModernButtonStyle}"
                            Width="90" MinHeight="30" />
                </Grid>

                <!-- List of Detected Sprites -->
                <Grid Grid.Row="3" Margin="0,0,0,8">
                    <DataGrid ItemsSource="{Binding Sprites}" 
                              AutoGenerateColumns="False" 
                              CanUserAddRows="False"
                              HeadersVisibility="Column"
                              Background="Transparent"
                              RowBackground="Transparent"
                              Foreground="{DynamicResource Brush.Text.Primary}"
                              BorderBrush="{DynamicResource Brush.Border.Separator}"
                              BorderThickness="1"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="{DynamicResource Brush.Border.Separator}">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="{DynamicResource Brush.Surface.MenuBar}" />
                                <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Muted}" />
                                <Setter Property="Padding" Value="8,4" />
                                <Setter Property="BorderThickness" Value="0,0,0,1" />
                                <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border.Separator}" />
                            </Style>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource Brush.Surface.Hover}" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource Brush.Surface.Hover}" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="8,4" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                <Setter Property="Background" Value="Transparent" />
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}">
                                <DataGridCheckBoxColumn.ElementStyle>
                                    <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Margin" Value="4,0,0,0"/>
                                    </Style>
                                </DataGridCheckBoxColumn.ElementStyle>
                                <DataGridCheckBoxColumn.EditingElementStyle>
                                    <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Margin" Value="4,0,0,0"/>
                                    </Style>
                                </DataGridCheckBoxColumn.EditingElementStyle>
                                <DataGridCheckBoxColumn.Header>
                                    <CheckBox IsChecked="{Binding DataContext.IsAllSelected, RelativeSource={RelativeSource AncestorType=DataGrid}, Mode=TwoWay}"
                                              IsThreeState="True"
                                              Content=""
                                              Style="{StaticResource {x:Type CheckBox}}"
                                              Margin="4,0,0,0"
                                              ToolTip="Select / Deselect All" />
                                </DataGridCheckBoxColumn.Header>
                            </DataGridCheckBoxColumn>
                            <DataGridTemplateColumn Header="Preview" Width="44" IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="#1E1E1E" Width="36" Height="36" CornerRadius="3"
                                                BorderBrush="{DynamicResource Brush.Border.Separator}" BorderThickness="1"
                                                Margin="2" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Image Source="{Binding PreviewImage}" Width="32" Height="32"
                                                   Stretch="Uniform" RenderOptions.BitmapScalingMode="NearestNeighbor"
                                                   RenderOptions.EdgeMode="Aliased"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" IsReadOnly="True" Width="*" />
                            <DataGridTextColumn Header="Width" Binding="{Binding Width}" IsReadOnly="True" Width="70" />
                            <DataGridTextColumn Header="Height" Binding="{Binding Height}" IsReadOnly="True" Width="70" />
                            <DataGridTextColumn Header="Frames" Binding="{Binding FrameCount}" IsReadOnly="True" Width="60" />
                            <DataGridTextColumn Header="Format" Binding="{Binding Format}" IsReadOnly="True" Width="100" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Empty State Placeholder -->
                    <Border Visibility="{Binding HasNoSprites, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Background="Transparent" IsHitTestVisible="False" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="No sprites detected in the selected file." 
                                       Foreground="{DynamicResource Brush.Text.Muted}" FontSize="12" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                            <TextBlock Text="Select a C/C++ or Python source file containing sprite arrays." 
                                       Foreground="{DynamicResource Brush.Text.Muted}" FontSize="11" Opacity="0.7" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Action Buttons -->
                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Click="BtnCancel_Click" Margin="0,0,8,0"
                            Style="{StaticResource ModernButtonStyle}"
                            Width="90" MinHeight="30" />
                    <Button x:Name="BtnImport" Content="Import Selected" Click="BtnImport_Click" IsEnabled="False"
                            Style="{StaticResource AccentButtonStyle}"
                            Width="140" MinHeight="30" IsDefault="True" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>